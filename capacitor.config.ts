import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.salones.sivitel',
  appName: 'AppSalones',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
